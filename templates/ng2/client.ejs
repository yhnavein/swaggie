@Injectable({
  providedIn: 'root'
})
export class <%= it.clientName.charAt(0).toUpperCase() + it.clientName.slice(1) -%>Service extends BaseService {
  <% it.operations.forEach((operation) => { %>
<%~ include('operation.ejs', operation); %>

<% }); %>
}


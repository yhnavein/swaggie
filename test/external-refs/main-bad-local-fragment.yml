openapi: 3.0.0
info:
  title: Bad Local Fragment
  version: 1.0.0
paths:
  /test:
    get:
      operationId: getTest
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '#bad-fragment'

openapi: 3.0.0
info:
  title: Invalid HTTP Ref
  version: 1.0.0
paths:
  /test:
    get:
      operationId: getTest
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: https://example.com/shared.yml#/components/schemas/User

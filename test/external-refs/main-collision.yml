openapi: 3.0.0
info:
  title: External Collision API
  version: 1.0.0
paths:
  /users:
    get:
      operationId: getUsers
      responses:
        '200':
          description: Users
          content:
            application/json:
              schema:
                $ref: ./components/collision.yml#/components/schemas/User
components:
  schemas:
    User:
      type: object
      properties:
        local:
          type: boolean
